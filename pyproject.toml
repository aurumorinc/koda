[project]
name = "koda-workspace"
version = "0.0.0"
requires-python = ">=3.9"
dependencies = [
    "camoufox>=0.4.11",
    "httpx>=0.28.1",
]

[tool.pdm]
resolution-locks = true
distribution = false

[tool.pdm.workspace]
members = [
    "packages/*",
    "apps/*"
]
